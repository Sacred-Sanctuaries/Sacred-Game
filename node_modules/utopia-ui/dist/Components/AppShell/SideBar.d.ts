/// <reference types="react" />
declare type route = {
    path: string;
    icon: JSX.Element;
    name: string;
    submenu?: route;
    blank?: boolean;
};
export declare function SideBar({ routes, bottomRoutes }: {
    routes: route[];
    bottomRoutes?: route[];
}): JSX.Element;
export {};
